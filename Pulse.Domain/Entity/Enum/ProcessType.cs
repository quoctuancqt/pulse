namespace Pulse.Domain.Enum
{
    public enum ProcessType
    {
        Restart,
        ShutDown,
        Login,
        Logout,
        ConnectionSlow,
        Disconnected,
        Connected,
    }
}
